# RISKS.md — live risk register

Seeded from `AGENT_BUILD_SPEC.md` §13. Owners assigned where the risk maps cleanly to one
workstream; joint where it doesn't. Update this any time a risk changes status — it's meant to be
read at every session start, not just written once. Append new risks as they're discovered; don't
silently resolve one without a note on how.

| ID | Risk | Impact | Owner | Mitigation | Status |
|----|------|--------|-------|-----------|--------|
| R1 | Provider RTT makes 200ms unreachable | 🔴 high | Joint | Phase 0 probe; two-track design; consider local generation | Open — probe not yet run (no API keys on either machine) |
| R2 | Deployment/mic fails late | 🔴 high | P | Deploy in Phase 1, redeploy every phase | Mitigated — live on Render since Day 1, verified with real STT audio |
| R3 | Rate limits break the benchmark run | 🟡 med | P | Retries in the bench script; run overnight if needed | Open |
| R4 | Index too large for host memory | 🔴 high | Joint | Cap at 200k chunks; measure RSS in Phase 1. **User has explicitly ruled out the paid fallback for now — free tier only, keep shrinking. Escalated back to the user 2026-08-18 (R-024) — the remaining lever's real cost turned out much worse than assumed, needs a team decision.** | **R's engineering side is genuinely done and verified: 727MB measured end-to-end in production-wired code (R-023), a real 61% cut from the 1,860MB starting point, zero quality cost.** But **R-024 (2026-08-18) measured the actual ceiling of the one remaining lever (corpus-shrink) before spending a re-ablation pass on it, and it's not the "bounded cost" it was assumed to be:** the embedder's own fixed session overhead (~388MB, confirmed inherent to loading e5-small's weights, not tunable — R-022) means an *empty* index would still cost ~467MB, 91% of the 512MB budget. Fitting the real, measured RSS-vs-chunk-count curve (linear, R²-tight — 78.7MB + 0.00262MB/chunk, verified against three real data points) to the remaining budget: **hitting 512MB needs ~17,300 chunks — an ~83% cut from the current 99,767.** Also checked whether a smaller embedder (Model2Vec's `potion-multilingual-128M`, no transformer session at all) could remove the fixed cost instead: **Recall@5 = 0.266 vs. e5-small's 0.652 on the same corpus — less than half** (`eval/ablation_ledger.csv`), so that's not viable either. **No ablation run, no corpus change made — this is a real, likely-severe quality tradeoff now touching graded requirements (C1-C6), and needs the user's/team's call, not a unilateral engineering decision.** Options on the table, none decided: (a) accept a smaller, quantified corpus cut and a deploy target above 512MB; (b) revisit paid-tier economics now that free-path RSS is 727MB vs. the original 1,474MB — a materially different cost-benefit than when the paid fallback was first discussed; (c) check whether a different free host offers more RAM headroom (P's domain). Runtime artifacts for the free-path option either way are ready: `embedder-lite-onnx-v1` + `index-metadata_aware-v2` GitHub Releases, P's Dockerfile change specified in `docs/DECISIONS_R.md` R-023. **User decided 2026-08-18: check option (c) first** — before accepting either a quality cut or the paid tier, verify whether a different free host (Fly.io, Railway, HF Spaces CPU, etc.) offers meaningfully more than Render free's 512MB RAM ceiling. **This is explicitly Workstream P's domain (deployment/hosting) — R has not investigated it and shouldn't; flagging here for P's session to pick up at its next start-of-session ritual.** If no better free option exists, the choice comes back to (a) vs (b) above. **P tried the direct approach first (2026-08-18, `docs/DECISIONS_P.md` P-020): deployed R-023's fix live on Render's free tier (user's separate direction, given directly to P: "try it, roll back if it crashes"). Real, live-verified result: `/healthz` survives, but every real `/ask` call OOM-kills the process — confirmed via runtime logs (no access-log line for the request before each restart), reproduced 3/3 on deliberate calls. Rolled back immediately (`git revert`), demo restored and re-verified (3/3 `/ask` calls 200 OK).** This sharpens the finding: the blocker is specifically *peak* memory during `_get_real_retriever()`'s first real load (index + embedder initialization), not just the 727MB *steady-state* figure — local testing under the same 512MB limit had shown steady-state fitting comfortably, so peak-during-load is the newly-identified real constraint. The hosting-alternatives check (user's directive to R's session) is still the next concrete step, now with a more precise target (needs headroom over the *peak*, not just 727MB steady-state). **2026-08-19: full component-by-component memory audit run (ADR-006), then one real, scoped fix applied on the user's explicit instruction — BM25/sparse index was loading unconditionally even in dense-only mode, never queried, costing ~105MB for nothing (ADR-006 finding). Fixed (ADR-007): `load_built_index_lean()` now only loads it for sparse/hybrid modes. Measured, 3 runs: steady-state RSS 778MB → ~715.7MB, a real ~63MB (8.1%) cut, zero quality cost, no architecture/corpus/embedding/FAISS/deployment change.** Gap to 512MB narrows from ~266MB to ~204MB — still open, corpus-shrink or paid tier remain the only levers with real headroom. **2026-08-19 (R-027): ran the real quality/latency/memory sweep R-024 never ran (recall was previously never measured at reduced corpus sizes), on user request.** Real 3-point full-stack measurement (20k/50k/99,767 chunks, apples-to-apples with the post-ADR-007 lean lookup): **corrects R-024's "~17,300 chunks / 83% cut" estimate — the real fit says ~5,900 chunks / ~94% cut**, worse than previously thought (R-024's fit combined two separately-measured numbers from different sessions; R-027's fit is 3 real full-stack points measured together, predicted-vs-measured within 0.6MB). Neither 20,000 nor 50,000 chunks (50,000 is exactly at ADR-002's spec floor) fits under 512MB (542.7MB and 606.4MB measured respectively) — **no spec-compliant corpus size (50k-200k, ADR-002) was measured to fit under 512MB, and the size that would (~5,900) is ~8.5x below the spec floor.** Real quality numbers now exist at the tested sizes too: Recall@5 crashes from 0.644 (full) to 0.406 (50k) to 0.184 (20k) — confirms the cut is not just memory-severe but quality-catastrophic well above the point that would even fit. **2026-08-19 (R-029/R-030): deep-dived the embedder itself instead of the corpus — found the tokenizer (not the ONNX model) was the dominant embedder cost (262MB vs. 137MB, R-028), verified a `sentencepiece`-based replacement reproduces 100.0000% exact token IDs on 1,020 real strings (R-029), then implemented it behind `LiteE5Embedder`'s unchanged interface.** Regression tests (`tests/index/test_lite_e5_embedder_tokenizer_regression.py`) caught and fixed a real trailing-whitespace edge case before it ever reached production. Retrieval eval reproduces the exact R-027 baseline (Recall@1/5/10, MRR@10 — MRR@10 matches to 16 significant figures) — confirms zero quality change. **Full production memory audit, 3 runs: steady-state RSS 715.7MB → 493.8MB average (-221.9MB, -31.0%), peak during startup+first-query 492.9MB — both now under Render's 512MB free-tier budget for the first time.** Total reduction across the whole memory-fix arc: 1,860MB → 493.8MB (-73.4%), zero quality cost, entirely R-owned engineering | Open (verification pending), high priority — R's engineering: 727MB→778MB→715.7MB→**493.8MB steady-state / 492.9MB peak (2026-08-19, R-030) — under the 512MB target locally for the first time.** Not yet verified in the actual target environment (P-020 found local-vs-Render can differ for peak-during-load specifically) — a real Docker `-m 512m` or live Render test is the honest next step before declaring this resolved. If confirmed, the corpus-shrink-vs-paid-tier decision may no longer be needed at all |
| R5 | MT artifacts in corpus hurt answer quality | 🟡 med | R | Spot-check in Phase 0; also the justification for G4 | Open — spot-check done, see `docs/DECISIONS_R.md` R-003, real artifacts found |
| R6 | Team disagreement on the 200ms interpretation resurfaces late | 🟡 med | Joint | Settled as ADR-004 (`t_pipeline` definition) | Mitigated — confirmed by both tracks at Day 1 sync |
| R7 | A member misses a promotion post | 🔴 high | Joint | Named grid in `SUBMISSION_CHECKLIST.md`, checked Aug 21 | Open |
| R8 | Polish commits break the latency number | 🟡 med | P | CI latency regression test from Phase 6 | Open |
| R9 | Scope creep into a second language before core is done | 🟡 med | Joint | Hard gate: not before Phase 7 exit | Open |
| P-R10 | Both dev machines run newer Python than the spec's 3.11+ target (R: 3.13, P: 3.14) | 🟡 med | Joint | Each `pyproject.toml`/venv verified real wheels exist for that Python before committing (R-001); watch ML-lib (faiss/onnxruntime/torch) wheel availability as versions climb | Open, monitoring |
| P-R11 | `FRIEND_BRIEF.md` referenced `frontend/reference/voice-rag-ui-preview.html` but the file was committed at repo root | 🟢 low | P | Moved via `git mv` on Day 1, see `docs/DECISIONS_P.md` P-004 | Resolved |
| P-R12 | Render (free tier) doesn't propagate our server-initiated WebSocket close frame promptly — connection lingers ~20-25s and eventually drops with code 1006 instead of a clean close | 🟢 low | P | Verified via a real TTS-generated speech round trip (`docs/DECISIONS_P.md` P-007) that `transcript_final` + `answer_final` both arrive in ~2.4s, well before the lingering-connection window — doesn't block real usage, only delays connection cleanup. Frontend now sends `{"event":"stop"}` proactively on `transcript_final`. Revisit if it ever becomes user-visible | Monitoring, not blocking |
| P-R13 | Sarvam's `/v1/chat/completions` endpoint hung indefinitely on every valid request for the first part of the session | 🟢 low | P | Confirmed transient — recovered later the same day (user's "retry later" call was correct). Re-probed: 30/30 → 4/30 failures, P50 TTFT 452ms. Full Track B path now verified working end-to-end for real. Kept as a closed record, not deleted — see `docs/DECISIONS_P.md` P-012 for the before/after numbers | Resolved 2026-08-18, same day |
| P-R15 | Sarvam's `response_format: json_schema` mode has a real bug with `array`-typed fields — the model fills preceding fields correctly then pads pure whitespace instead of emitting the array, consuming all of `max_tokens` and never terminating (`finish_reason: "length"`, `content` truncated). Isolated by removing the array field alone and observing `finish_reason` flip to `"stop"`. Also: `sarvam-105b-conversations` (the "voice-agent-tuned" variant) is broken for structured output entirely — produces pure whitespace immediately regardless of schema shape; plain `sarvam-105b` works correctly. **Update 2026-08-18 (P-017): the array type was A cause, not THE cause** — see P-R20, the same whitespace-padding symptom recurs with an all-string schema | 🟡 med | P | Worked around, not fixed upstream: `cited_chunk_ids` is now a comma-separated string in the schema, not a JSON array (`docs/DECISIONS_P.md` P-013). Switched model from `sarvam-105b-conversations` to `sarvam-105b`. If Sarvam fixes this server-side, the workaround can be reverted, but there's no cost to leaving it — a CSV string round-trips fine | Worked around 2026-08-18; root cause broadened, see P-R20 |
| P-R16 | `sarvam-105b` is a reasoning model: by default every response includes a `reasoning_content` chain-of-thought that's billed and counted against `max_tokens` *before* the real answer — a short token budget can be entirely consumed by reasoning, leaving `content: null`. Sarvam's own docs recommend `reasoning_effort: null` for latency-sensitive/live-call use. **Update 2026-08-18 (P-017): `reasoning_effort: null` only disables Sarvam's separate hidden CoT mechanism — it does NOT bound how verbose the model is inside our own schema's `reasoning` field**, which independently consumed the full budget under a realistic multi-chunk context | 🟢 low | P | Fixed for the hidden CoT mechanism: `reasoning_effort: null` added to every Track B request (`docs/DECISIONS_P.md` P-013). Separately fixed for our own schema's `reasoning` field: `_SYSTEM_PROMPT` now explicitly asks for one short sentence (P-017), verified to cut reasoning-field length ~5-8x and successful-call latency from ~2.2-2.7s to ~1.0-1.2s | Resolved 2026-08-18 |
| P-R17 | Track B's non-streaming full-completion time is highly variable (~1.4s to 15s+ observed across repeated identical calls), well over both the 110ms target and the P50 452ms TTFT the probe measures — because non-streaming mode waits for the entire ~500-token structured response, not just the first token, and generation speed itself varies with provider load. This isn't a bug; it's the direct, expected cost of the "non-streaming for now" design choice (`docs/DECISIONS_P.md` P-013) | 🟡 med | P | **Superseded 2026-08-18 (P-017): switched to streaming.** Track A still covers the default-budget case as before, but streaming now also bounds the *failure* case specifically — see P-R20 | Superseded by P-017/P-R20 |
| P-R20 | (P-017, sharpened P-019) Sarvam's `strict: true` json_schema mode has a broader reliability problem than P-R15's array-field diagnosis captured: even with an all-string schema and a brief-reasoning prompt, the model sometimes completes the earlier fields correctly, then fails to continue to the final field and close the object — padding whitespace toward `max_tokens` instead. **P-019 found a much sharper correlation, not just a general rate: the stall strongly correlates with "insufficient context" (`needs_more_context: true` / "I don't know"-style) answers specifically** — 12/12 attempts failed this way across several context shapes and both the old 3-field and new 4-field schema (confirming the field addition isn't the cause), every time immediately after a correctly-reasoned "the context doesn't contain this" answer. This means Track B's own abstention/insufficient-context branch — exactly where a nuanced generative answer matters most — is the *most* likely case to hit this bug, not a random subset | 🟠 med-high | P | Mitigated, not fixed: streaming + stall detection (`STALL_THRESHOLD_CHUNKS=20` consecutive whitespace/empty deltas) detects this pattern and aborts in ~a few hundred ms instead of waiting out the full `max_tokens`/timeout budget (`docs/DECISIONS_P.md` P-017). Track A covers every case where Track B fails, verified live — this is a real quality/contribution-rate gap for Track B, not an availability gap; the system stays correct throughout. Now has a much more precise, highly reproducible repro case (insufficient-context query, any chunk count, `strict: true` schema) worth reporting to Sarvam | Open — cost mitigated 2026-08-18, root cause is provider-side, not ours to fix; repro case sharpened 2026-08-18 (P-019) |
| R-R14 | A3 found hybrid (RRF) retrieval *regresses* quality vs. dense-only on this corpus (`docs/DECISIONS_R.md` R-010, `docs/EVAL_RESULTS.md` §3) — root cause: BM25's weaker top ranks get equal RRF fusion weight against dense's stronger ones, with only a `top_k=10` candidate pool per lane. A larger per-lane candidate pool before fusion (e.g. top-50, truncated to top-10 after) is a standard mitigation, not yet tested — would be a second, separate ablation axis from retrieval mode | 🟢 low | R | **Tested 2026-08-18 (`docs/DECISIONS_R.md` R-026):** candidate pools of 10 (baseline)/30/50/100 all held Recall@5 in the 0.578-0.604 range, flat-to-slightly-worse than the pool=10 baseline, and every hybrid config stayed well below dense-only's 0.652 regardless of pool size. The mitigation doesn't work — larger pools admit more of BM25's weak candidates at full RRF weight, diluting rather than helping. Dense-only remains correct, no config change | **Resolved 2026-08-18 — tested, doesn't help, closed with a real answer instead of left untested** |
| P-R18 | `retrieve()`'s production wiring switched to dense-only (`docs/DECISIONS_R.md` R-010 update), which changes `RetrievedChunk.score`'s scale from RRF-fused (~0.008-0.033, structurally below G3's `TAU=0.35`) to raw cosine similarity (~0.3-0.95) — the scale `g3_confidence.py`'s own docstring says `TAU` was already chosen for. **Confirmed by direct test, not just inferred:** re-ran `test_ask_returns_answered_for_a_query_the_stub_covers`'s real query against the live `/ask` endpoint post-switch — it still abstains, but the `refusal_reason` changed from what would have been a `top1 < TAU` failure (structurally guaranteed under the old RRF scale) to `"Ambiguous match: top result doesn't clearly stand out"` (the *margin* check, `top1 - top5 < MARGIN=0.05`) — i.e. `top1` now clears `TAU` as intended, and G3 is abstaining on a real, if still-uncalibrated, ambiguity signal instead of a structurally-broken one | 🟡 med | P | Score-scale mismatch fixed by the dense-only switch itself. Real G3 calibration has now run — see R-R19 below for the result, which is more consequential than this row's original scope | Superseded by R-R19's calibration data, 2026-08-18 |
| R-R19 | Real G3 calibration (`docs/DECISIONS_R.md` R-015, `eval/calibration.json`, `docs/assets/g3_calibration.png`) found `docs/EVAL_PROTOCOL.md`'s targets (false-refusal<10% AND correct-refusal>80%) are **not simultaneously reachable** via pure top1-cosine `TAU` gating on this corpus: at 10% false-refusal, correct-refusal is only 38%; reaching 75-79% correct-refusal needs 19-30% false-refusal. Root cause verified (not assumed): MSMARCO-XI passages aren't unique per query_id, so genuinely out-of-index queries often still retrieve a topically-close or even coincidentally-correct passage (near-duplicate general-knowledge content recurring across the full ~780k-row dataset), making cosine similarity alone an imperfect signal for "no real answer exists." This is a real product tradeoff (how often is refusing a real question acceptable vs. confidently answering with a weak match), not a data question R can settle alone | 🟡 med | Joint | Balanced operating point (weighs both `EVAL_PROTOCOL.md` targets equally): `TAU=0.8835` applied to `src/vrag/guardrails/g3_confidence.py` (`docs/DECISIONS_P.md` P-015). Follow-up completed same day (`docs/DECISIONS_R.md` R-017): `MARGIN=0.05` (the pre-calibration placeholder) did **not** carry over to the new `TAU` — verified live it caused 88.0% false-refusal, not the intended 19.3%, because in-domain top1-vs-top5 gaps are naturally tiny at this operating point. Fine sweep confirmed no useful non-zero `MARGIN` exists at this `TAU`; set `MARGIN=0.0` (empirically correct, not a shortcut), confirmed live (2/3 previously-blocked test queries now answer correctly). `test_g3_confidence.py` updated: one test now confirms the margin gate is a deliberate no-op at this operating point, another confirms the mechanism itself still works via `monkeypatch` for any future recalibration | **Resolved** 2026-08-18 — TAU and MARGIN both applied and verified live, 151/151 project tests green |
| R-R21 | **The live deployment (`https://vrag-voice.onrender.com`) runs entirely on the Day-0 retrieval stub — none of A1-A4/efSearch/G3's real work has ever reached the public URL.** Found while checking whether `docs/TEAM_SPLIT.md` §5's Day-3 "complete" milestone was actually met before starting Day-4-scoped work — local testing on both dev machines showed the real pipeline working, but the deployed URL itself was never directly checked. `POST /ask` against a real corpus query returns `chunk_id: "stub-chunk-001"` with `retrieve` timing `0.007ms` (`docs/DECISIONS_R.md` R-018). Root cause: `Dockerfile` runs bare `pip install -e .` (no `[retrieval]` extra — `sentence-transformers`/`faiss-cpu`/`bm25s`/`torch` never installed in the image), and `data/` is gitignored so the built index was never going to reach the container regardless. `AGENT_BUILD_SPEC.md` §5.3 explicitly anticipated this ("build offline, commit to a release asset or object storage, download-and-mmap at boot") — that step was simply never built by either track | 🔴 high — blocks the real C7 deliverable ("live working link") if unresolved before submission | P (deployment ownership, `docs/TEAM_SPLIT.md` §2) | **P applied R-023's 3-step Dockerfile fix 2026-08-18 (`docs/DECISIONS_P.md` P-020)**, fixed two real bugs found along the way (tarball path mismatch needing `--strip-components=1`; `numpy>=2.5` needs `python:3.12-slim`, not `3.11-slim`), and verified locally under Docker's own `-m 512m` limit: 446.8MiB steady-state, real (non-stub) retrieval confirmed, deployed live on that basis with a documented rollback plan. **R verified the live URL directly right after (R-025, 2026-08-18), same "don't trust a deployed claim" discipline that found this row originally: `GET /healthz` → 200 OK (3/3 retries), but `POST /ask` with a real query → 502 Bad Gateway, `x-render-routing: no-deploy`, reproduced twice.** Pattern (health check fine, first real-retrieval-triggering request fails) is consistent with the OOM-on-Render-but-not-local-Docker scenario P-020 already flagged as a real possibility — not confirmed as root cause (no Render dashboard/log access from here), but the live URL was verified broken for real answers at that point, not a projection. **P's session executed the documented rollback minutes later** (`8b04033`, reverting `f5feb31`) — R re-verified live immediately after: `/healthz` 200 OK, `/ask` back to `status: "answered"` serving `stub-chunk-001` (the Day-0 stub), stable, no more 502s. **Net result: live URL is safe and functional again, but back on the stub — R-023's real-retrieval-in-production fix is verified correct in isolation (R-023) and in local Docker (P-020), but not currently live.** Getting real retrieval onto the live URL still needs P to diagnose the actual Render-vs-local-Docker memory discrepancy (real OOM? different limit enforcement? something else?) before re-attempting — R has no Render log/dashboard access to help narrow that down further | Open, high priority — live URL safe (serving the stub) as of 2026-08-18 after P's rollback; real retrieval verified twice over (R-023, P-020) but still blocked from reaching production by an unexplained Render-specific memory discrepancy |

## Blockers found this session (Day 0-1, 2026-08-17)

- **No Sarvam / Groq API keys present on either dev machine.** Blocks `scripts/probe_latency.py`
  (R1/ADR-003) and any real STT/LLM call from R's side. Owner: whoever holds the keys — flagged to
  the user, not guessed around. Everything that doesn't need a live provider call (dataset work,
  chunking, index build, offline eval) proceeds in the meantime. (Note: Workstream P *does* have a
  working Sarvam key, used for the live deploy's real STT — see `docs/DECISIONS_P.md` P-007. R's
  local probe script still needs its own key to run.)
- **IPv6 is broken on this dev machine's network (Workstream R's machine)** (confirmed via `curl`:
  IPv6 to huggingface.co resets the connection; IPv4 works). Not a project risk per se, but worth
  knowing if any HTTP client hangs unexpectedly during dataset download — force IPv4 if so. Further
  wrinkle found: HuggingFace's "Xet" transfer backend for large files does its own networking that
  bypasses a process-wide IPv4 DNS patch — had to be disabled (`HF_HUB_DISABLE_XET=1`) to get a
  reliable download. See `scripts/_netcompat.py`.
