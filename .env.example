# Copy to .env and fill in. .env is gitignored — never commit real keys.

# Sarvam AI — STT (required, C1) and optionally LLM/TTS
SARVAM_API_KEY=

# Generation provider fallback candidates (Phase 0 probe decides which is primary)
GROQ_API_KEY=

# Deployment
PORT=8000
